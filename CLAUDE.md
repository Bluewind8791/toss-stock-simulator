# CLAUDE.md

토스증권 Open API 기반 **라오어 무한매수법 시뮬레이터**.
프로젝트 개요·참고 링크는 [README.md](./README.md) 참조.

## 도메인 규칙 (무한매수법)

버전별 상세 수식은 README.md 참조. 여기엔 **구현 불변식**만 적는다.

- **T** = 현재 사이클의 진행 회차. 매수 체결로 1회분을 소진할 때 증가.
- **1회 매수금액** = 사이클 시드 / 분할수 (v2.2=40, v3.0=20)
- **평단** = 누적 매수금액 / 누적 수량. 체결마다 갱신.
- **LOC 매수 모사** = 해당일 종가 ≤ 주문 지정가일 때만 그 종가로 체결, 아니면 미체결.
- **LOC 매도 모사** = 해당일 종가 ≥ 주문 지정가일 때만 그 종가로 체결.
- **지정가 매도 모사** = 해당일 고가 ≥ 목표가일 때 목표가로 체결.
- **쿼터 손절/쿼터모드** = 분할 소진 시 보유 수량 1/4을 시장가(해당일 종가) 매도.
- 사이클 종료(전량 매도) 후 확보된 현금으로 새 사이클 시작.
  v2.2는 단리(원 시드 고정), v3.0은 수익금 반영(복리).

**하드코딩 금지 — 파라미터로 노출**: 버전, 분할수, 익절률, 대상 종목, 시드, 단·복리 여부.

## 토스 API 사용 원칙

- 인증: OAuth 2.0 Client Credentials → Bearer access token
- 현재 단계는 **시뮬레이터 = 읽기 전용**. 주문 생성/수정/취소 API는 호출하지 않는다.
  (추후 실주문 연동은 별도 단계 — 지금은 범위 밖)
- 자격증명(client id/secret)은 `.env`에만. 커밋 금지.
- 시세 조회는 캐시/로컬 저장 우선 — 백테스트 반복 실행 시 API 재호출 최소화.

## 미확정 사항

- **v2.1 세부 수식** 미확정. 2차 출처엔 "전·후반전 분리 + 일부 물량 +5% 익절"까지만 있음.
  구현 전 원저작(서적) 기준으로 확인할 것. 추측으로 채우지 말 것.
- 토스 API에 **LOC 주문 타입이 문서상 없음**. 실주문 연동 시 대체 방식 필요.

## 코드 규칙

- 시뮬레이션 엔진(순수 계산)과 API 클라이언트(I/O)는 분리한다.
  엔진은 시세 배열만 받아 동작 — 네트워크 의존 없이 테스트 가능해야 함.
- 금액 계산은 부동소수점 오차에 주의. 수량은 정수.
